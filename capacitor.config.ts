import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eShopping',
  appName: 'eShopping',
  webDir: 'www',
  plugins: {
    LocalNotifications: { }
  }
};

export default config;
